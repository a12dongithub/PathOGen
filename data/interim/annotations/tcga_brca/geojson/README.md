# Nucleus GeoJSON annotations

Expected contents are one GeoJSON file per TCGA tile with matching filename stem,
nucleus polygons, predicted class names, confidence when available, and a
sidecar recording CellViT++ model/checkpoint provenance.

Three matching GeoJSON files are present locally for the condition-building
smoke test and are ignored by Git. The complete annotation cohort and its
CellViT++ provenance are not present.
