# Derived classifier features

This directory is reserved for task-specific feature tables that are not encoder embeddings. `morphology_with_counts.parquet` is currently missing; generate it with `experiments/representation_audit/38_generate_cell_counts.py` after the canonical 16-value morphology table and GeoJSON annotations are available.
