# TCGA UNI2-h embeddings — not separated

Store configured UNI2-h embeddings here. Historical combined features remain in
`../combined/benchmark_features.parquet` until their provenance is validated.
