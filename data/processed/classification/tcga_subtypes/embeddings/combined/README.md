# Historical combined benchmark features

`benchmark_features.parquet` combines multiple encoder outputs. Preserve it
until its sample cohort, preprocessing, encoder revisions, and columns are fully
validated; new runs should write model-specific embeddings to sibling folders.
