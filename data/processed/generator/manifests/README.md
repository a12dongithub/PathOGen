# Generator manifests — missing

Expected outputs include a tile/slide/patient manifest, split assignment,
`metadata.jsonl` when required by the Phase-1 image-folder loader, and hashes of
the tiles, annotations, conditions, feature scaler, and preprocessing config.
