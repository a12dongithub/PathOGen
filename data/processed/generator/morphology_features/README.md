# Morphology/stain features — missing

Expected outputs:

- `morphology_raw.parquet`;
- `morphology_standardized.parquet`;
- `scaler.joblib` fitted on training patients only; and
- `feature_manifest.json` with ordered columns and split/config hashes.

The historical canonical morphology table and fitted morphology scaler are not
present in this workspace.
