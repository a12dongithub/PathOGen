# Evaluation runs

Each Workflow 06 run owns one directory here, for example
`phase2_1000_seed42/`. It contains generated and real tiles, grids, the
manifest, FID/KID, and Workflow 07's `control_consistency/` artifacts.
