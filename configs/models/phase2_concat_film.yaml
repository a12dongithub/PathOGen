schema_version: 1
status: active_workflow_05_checkpoint_contract
architecture: direct_concat_spatial_encoder_plus_film
base_model: Manojb/stable-diffusion-2-1-base
resolution: 512
prompt: he
latent_channels: 4
spatial_input_channels: 5
spatial_output_channels: 4
unet_input_channels_after_concat: 8
morphology_condition_dimension: 16
film_clamp:
  minimum: -0.5
  maximum: 0.5
notes:
  - This describes the supported Workflow 05 checkpoint, not ControlNet.
  - The inference bundle includes the frozen VAE, tokenizer, text encoder, and scheduler.
