# Configuration

This directory is reserved for portable data, model, training, evaluation, and experiment configuration files. The migrated scripts still contain embedded defaults and machine-specific absolute paths; those settings have not been guessed or silently rewritten.

Add a configuration only after tracing the corresponding historical script and recording its dataset, checkpoint, split, preprocessing, seed, and output contract.
