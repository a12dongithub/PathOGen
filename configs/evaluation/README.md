# Evaluation configuration

Publication evaluations should add immutable configuration files here containing a held-out patient/slide manifest, architecture label, checkpoint hash, preprocessing/scaler bundle, sample count, seed list, generation settings, and metric-library versions. Historical scripts do not yet consume a shared evaluation configuration.
