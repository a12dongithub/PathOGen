# Miscellaneous model artifacts

`checkpoint_archives/` contains retained ZIP archives that are not directly runnable checkpoints. Extract and verify an archive into an appropriately named `artifacts/runs/<run-id>/checkpoints/` directory before use.
