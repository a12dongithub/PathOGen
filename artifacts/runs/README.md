# Workflow runs

New Workflow 05 outputs belong in timestamped subdirectories here. Each run
contains `run_manifest.json`, `pairs.jsonl`, and generated images. Historical
results were moved to `archive/artifacts/historical_runs/`.
