# Archived checkpoint

Extract the verified 10,000-step phase-2 checkpoint here as `checkpoint-10000/` before using the legacy evaluation script. The source ZIP is in `artifacts/misc/checkpoint_archives/`.
