# NuHTC demo assets

The upstream `image_demo.py` source remains here. Large example images, WSI files, and historical inference outputs were moved to `data/misc/nuhtc_demo/`. The PanNuke checkpoint was moved to `artifacts/models/third_party/nuhtc/pannuke.pth`.
