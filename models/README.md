# Local models

Keep the frozen checkpoints required by active workflows here:

```text
models/
├── cellvit_plus_plus/cellvit_sam_h_x40_amp_001/model.pth
└── pathogen_phase2/checkpoint_30000/
```

Weights remain local and are ignored by Git.
