# Build conditions

The migrated implementations are `cpathogen.preprocessing.spatial_maps` and `cpathogen.preprocessing.morphology_features`. This workflow still needs a portable orchestrator, split-safe scaler serialization, and round-trip validation.
