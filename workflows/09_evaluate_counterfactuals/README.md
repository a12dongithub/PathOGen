# Evaluate counterfactuals

Historical evaluation logic is grouped under `experiments/` and `src/cpathogen/evaluation/`. A portable workflow still needs held-out manifests, repeated seeds, control-fidelity checks, and patient-level statistics.
