# Train classifiers

Logistic regression, random forest, XGBoost, and MLP training remains embedded in historical experiment scripts. This workflow requires sealed patient/slide splits and model/provenance sidecars.
