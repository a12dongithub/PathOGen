# Extract embeddings

UNI2-h, CTransPath, and ResNet50 extraction logic remains embedded in scripts under `experiments/`. Extract shared, configured adapters into `src/cpathogen/encoders/` before treating this as a portable workflow.
