# Experiment outputs

Generated images, CellViT++ GeoJSON predictions, CSV summaries, confidence intervals and manifests are written here by default. Use a Google Drive path with `--output-root` if results must survive a Colab runtime reset.
