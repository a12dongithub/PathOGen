# CellViT++ checkpoint

Place a CellViT++ segmentation checkpoint here, preferably:

```text
CellViT-256-x40-AMP.pth
```

Alternatively, pass `--cellvit-model /content/drive/.../model.pth` or `--cellvit-model-url <Google-Drive-link>` to the setup script.
