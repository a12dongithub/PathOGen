# 512 final dataset

Expected runtime contents include:

- `images/`
- `spatial_maps/`
- `geojsons/`
- `morphology_stats.parquet` or `morphology_features/morphology_stats.parquet`

The setup script can download and locate these automatically even when the Drive ZIP contains additional parent folders.
