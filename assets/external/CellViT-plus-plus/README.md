# CellViT++ source

The setup script sparse-clones the required `cellvit/` package into the ignored `repository/` child directory. The experiment configuration records its resolved path as `cellvit_root`.
